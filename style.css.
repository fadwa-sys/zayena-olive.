.product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    padding: 40px;
}

.product-card img {
    width: 100%;
    border-radius: 15px;
    object-fit: cover;
}
